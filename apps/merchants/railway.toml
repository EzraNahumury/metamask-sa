# Railway service config for the merchants mock.

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/merchants/Dockerfile"

[deploy]
startCommand = "pnpm --filter @delegate/merchants run start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
